# Запуск на одной машине

В первом терминале `./run_single.sh`, затем `./run_server_single.sh`

Во втором терминале `./run_client_single.sh`

# Запуск в локальной сети

Доработать, чтобы в `client_boost_asio.cpp` было достаточно передавать в аргументы `main()` IP и номер хоста.

Доработать `run_client.sh`, чтобы было достаточно передавать аргументами два IP и номер хоста

Запуск сервера: `./run_server.sh`

# Расчёт дельт и построение графиков

Доработать, чтобы в параметры `calc_deltas.py` достаточно было передавать названия файлов собранных дельт сырых сокетов и бустовых.

Перед запуском в файле указать названия файлов сырого и бустового решения. Запуск: `python3 calc_deltas.py`
